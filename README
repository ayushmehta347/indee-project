To run the code ->
  install the required packages
  Right click on main.js file and open it in terminal
  Write npm run serve in termianl and code will run.

The project is also deployed on github, i have provided the link while filling out the submission form. 


